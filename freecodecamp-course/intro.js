// TypeScript is a Developer Language, it helps to build safety JS applications, in the end, everything is JS.
//Variables
var num = 5;
var str = 'Hello';
var bool = false;
var arr = ['a', 'b', 'c', 'd'];
var user = { name: "Vandilson", age: 10, isAdmin: false };
// Functions
function add(value1, value2) {
    return value1 + value2;
}
add('2', 3); // it accepts
function add2(value1, value2) {
    return value1 + value2 + '2';
}
add(2, 3); // it accepts// it accepts
//Right Way
function add3(value1, value2) {
    return value1 + value2;
}
